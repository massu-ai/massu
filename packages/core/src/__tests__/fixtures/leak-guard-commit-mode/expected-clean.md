<!-- leak-guard fixture for plan-leak-guard-range-mode-verify P-B-001 (control case) -->
# Clean Fixture — Control Case

This fixture contains NO leak triggers. The leak-guard scanner should PASS on this file in all modes (staged, tree, commit, content-trigger).

Generic prose about software engineering: feature flags, gradual rollouts, observability, error budgets, structured logging, immutable infrastructure, declarative configuration.
